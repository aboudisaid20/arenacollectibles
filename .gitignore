node_modules
.next
out
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env*.local

# local database
.data

# admin-uploaded product images now live in .data/uploads, on the same
# persistent volume as the database — see src/lib/storage.ts
